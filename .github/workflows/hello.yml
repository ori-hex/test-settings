name: Lint
on:
  check_run:
    types: [rerequested]
jobs:
  read-data:
    name: "Read data"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout branch"
        uses: actions/checkout@v2
      - name: "Read"
        run: cat hello.txt 
